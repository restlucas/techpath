import { Metadata } from "next";

export const metadata: Metadata = {
  title: "configurações | techpath",
};

export default function SettingsPage() {
  return (
    <section>
      <h1>Hello settings</h1>
    </section>
  );
}
